{
    "scripts": {
        "watch": "nodemon -e ts -w index.ts -x npm run watch:serve",
        "watch:serve": "ts-node --inspect index.ts",
        "tsc": "tsc",
        "dev": "ts-node-dev --respawn --transpile-only ./src/index.ts"
    },
    "devDependencies": {
        "nodemon": "^2.0.15",
        "ts-node": "^10.4.0",
        "typescript": "^4.5.2"
    },
    "dependencies": {
        "ts-node-dev": "^1.1.8"
    }
}
